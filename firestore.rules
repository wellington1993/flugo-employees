rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Liberando acesso total para eliminar bloqueios de permissão definitivamente
      allow read, write: if true;
    }
  }
}
